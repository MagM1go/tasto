[tool.uv]
package = true

[project]
name = "tasto"
version = "0.1.0"
description = "Interesting network library for Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "nox>=2026.4.10",
    "pip-audit>=2.10.1",
    "ruff>=0.15.20",
]
